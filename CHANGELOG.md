# 更新日志

## v1.2.2
修复 v1.2.1 造成的「点不开面板」：
- v1.2.1 把拖曳的 pointermove/pointerup 移到 document 上，但悬浮球仍然在 pointerdown
  时呼叫 `setPointerCapture`。指针捕获会改变事件的派送路径，导致挂在 document 上的
  `pointerup` 收不到事件，连带把点击一起吃掉 —— 结果球既拖不动也点不开。
- 改成**拖曳与点击彻底分开**：开关面板走原生 `click`（最可靠），pointer 事件只负责拖曳，
  真的拖动过才设 `suppressClick` 让紧接着的那次 click 不误触发。完全不再使用指针捕获。
- **修掉 pagehide 隐患**：手机切分页或切到别的 App 时浏览器也会发 `pagehide`
  （进 bfcache，之后还会回来），v1.1.1 以来这会把整个 UI 拆掉，而且 `destroyed`
  旗标是永久的、回来后再也不会重建。现在会检查 `event.persisted`，
  bfcache 那种情况不拆 UI，只有真正卸载/停用脚本才清理。

## v1.2.1
拖曳修复：
- **触屏拖不动**：悬浮球和面板标题栏加上 `touch-action: none`。
  没有这行时浏览器会把拖曳手势判定成卷动，直接发 `pointercancel` 取消掉。
- **面板拖不动**：边界计算有 bug——面板宽度是 `100vw-16px`，在窄屏上几乎等于画面宽，
  `Math.max(0, Math.min(innerWidth - w, x))` 算出来的可移动范围只剩 16px，等于锁死。
  改成面板比画面宽时允许拖到负座标。
- 拖曳的 move/up 监听改挂到 document 上，手指或鼠标移出元件外也不会断。

## v1.2.0
修好注入 + 手机/窄屏支持：
- **修复世界状态注入失败**（`Cannot read properties of undefined (reading 'IN_CHAT')`）：
  SillyTavern 的 `extension_prompt_types` 常量其实没有挂在 `getContext()` 上，
  改成直接用 ST 源码里的数值（IN_CHAT=1 / SYSTEM=0），若将来 context 有导出会优先用导出的。
  在此之前世界状态**完全没有注入到RP**，这版才真正生效。
- **触屏可以拖曳了**：拖曳从 mouse 事件改成 Pointer Events，手机/平板都能拖动悬浮球和面板。
  移动超过 5px 才算拖曳，否则视为点击开关面板。
- **窄屏自适应**：面板宽高改成 `min(560px, 100vw-16px)`，不会再撑出画面；
  屏幕宽度 420px 以下时左侧分页栏自动缩成图标条。
- 悬浮球位置在启动时会夹回可视范围，换装置或改视窗大小后不会卡在画面外看不到。
- 面板开启位置改成先量实际尺寸再定位，放不下会自动改放另一侧或居中。

## v1.1.1
修复与健壮性：
- **修复贴上 loader 后没有反应**：本体是被 import() 载入的独立模块，酒馆助手注入到
  「脚本作用域」的裸函数（getVariables / eventOn 等）在模块里不一定看得到，
  会直接 ReferenceError 导致整个脚本静默失败。改为统一从 window / window.TavernHelper
  解析接口，缺哪个就在主控台点名，不再整包炸掉。
- 启动失败时画面右上角会跳红色错误框，不再是什么都没有
- **停用脚本时自我卸载**：监听 pagehide，关掉脚本后悬浮球会自己消失，
  并清掉注入到RP的世界状态，不用重开酒馆
- 找不到 jQuery / tavern_events 时给出明确警告

## v1.1.0
UI 重构 + NPC 关系演化系统：
- **圆形悬浮球**：可拖曳、位置会记忆，点击展开面板（拖动不会误触发开关）；
  模拟运行中悬浮球会旋转
- **左分页 + 右主页**布局，面板本身也可拖曳
- 五个分页：🧑 NPC 状态 / 💞 关系网 / 🌍 世界大小事 / ⚙️ 设置 / 🛠 高级·日志
- **NPC 状态页**：列出所有追踪中的 NPC，点名字展开看位置/行为/互动对象/近况/关系网，
  可直接从卡片移出追踪
- **关系网页**：关系变动时间线（谁和谁从什么变成什么、为什么）+ 目前关系一览
- **NPC 之间会自己发展关系**：交往、结婚、成为闺蜜/好兄弟、赌气、冲突、绝交等，
  关系带状态标签并依类型上色；关系循序渐进不可跳级，且强制双向一致
  （AI 只写单边时程序会自动补齐反向关系）
- **人生阶段照实记录**：怀孕、生育、子女都会正常写进状态台账，
  AI 不会回避或淡化这些事；NPC 卡片上以 🤰/👶 标示，展开可看详情。
  （只记录客观状态，不描写过程）
- 关系状态与最近的关系变动也会一起注入正式RP（可在设置里关掉）
- 注入深度改成可在面板调整

## v1.0.0
首个 git 发布版本：
- 悬浮面板（拖拽/折叠）
- 双 API 模式（沿用 ST 连线 / 自定义反代）
- 楼层数 / 分钟数触发
- 重要角色追踪（位置/活动/互动对象），排除路人/店员等打酱油角色
- 世界大小事、玩家过往行为后续发酵追踪
- 时间锚点同步（读取预设 TimeLocation 变量）
- 世界书条目名称参考（减少 AI 瞎编地名）
- 历史自动压缩（事件/发酵记录过多时自动归档）
- 世界状态注入正式RP（对齐预设"现实层"`<World_Current>`插槽，只注入状态不注入指令）
- 手动修正面板（JSON 直接编辑）
- 角色升降级（手动加入/移出追踪名单）
- 改用 loader.js + jsDelivr 动态加载架构，更新只需改 tag 版本号
